# Core backend logic only (no Flask/UI dependencies needed)
#
# NOTE: these are now also listed in backend/requirements.txt, since
# crawler.py and whois_lookup.py run in-process inside the main FastAPI
# app (app/dispatcher.py), not just as standalone scripts. Keep both
# files in sync if you add a new analyzer dependency here.

# crawler
requests
beautifulsoup4

# pdf_analyzer
pymupdf
pillow
pyzbar
# pyzbar wraps the system `zbar` shared library -- it is NOT pulled in
# by pip. Install it via the OS package manager, e.g.:
#   Debian/Ubuntu: apt-get install -y libzbar0
#   macOS (brew):  brew install zbar
# Without it, QR_DECODING_AVAILABLE is False and QR-code detection is
# silently skipped rather than crashing the analyzer.

# whois
python-whois
dnspython

# image-analyzer
# Note: also requires the `exiftool` binary installed on the system
# (e.g. `apt-get install libimage-exiftool-perl`), it is not a pip package.
groq
tldextract>=5.1,<6.0
lxml
pytest>=8.0,<9.0
uvicorn==0.49.0
fastapi==0.138.0
pydantic==2.13.4
python-dotenv==1.2.2
python-multipart==0.0.32
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.0
certifi==2026.6.17
click==8.4.1
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
packaging==26.2
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
dnspython
requests
reportlab
dnstwist
